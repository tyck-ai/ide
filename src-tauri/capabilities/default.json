{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all workspace windows",
  "windows": ["workspace-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
